export class GeneralVisits {
    public dates: string[];
    public visits: number[];

    constructor(dates: string[], visits: number[]) { 
        this.dates = dates;
        this.visits = visits;
    }
}